---
name: folia-glossary
description: Folia 一键可视化功能相关术语表(glossary)
---

# Folia 一键可视化 — 词汇表

> 本文件只记录**业务概念**。实现决策、技术栈、模块边界写到 ADR,不在此列。

## 已确认

- **源对象 (Source Document)**:用户当前在 Folia 中查看/编辑的 Markdown 文档整体。"一键可视化"的输入默认指向**整篇文档**,而非某一选区或某一块。
- **结构化中间表示 (Structured Intermediate Representation, SIR)**:从非结构化 Markdown 抽取出的、供渲染层消费的中间结构。任何可视化都先把源对象归约到这里,再决定画什么。
- **可视化产物 (Visual Artifact)**:根据源文档派生出的独立、可编辑图形结果。它不改写 Markdown；事实或原文抽取错误应回到源文档修正，布局、视觉层级、简称和展示文案只修改可视化产物。
- **Folia**:面向办案/法律知识工作场景的桌面端 Markdown 工具(Tauri + Vditor + React 19)。它不是一个简单 Markdown 阅读器——已具备脑图画布 (`MindMapPane`)、自由流图层 (`FreeFlowLayer`)、自研预览服务 (`vditorPreviewChromeService`)、Mermaid/KaTeX 内置渲染、Vditor IR 清洗、Word/HTML/PDF 导出管线。
- **独立派生视图 (Derived Visual View)**:不写回源文档的可视化工作区。它保存自身的视图类型、结构、布局和展示文案，并保留与源文档内容的来源关联。
- **Folia 可视化文件 (`.foliaviz`)**:可保存、可移动并可继续编辑的可视化产物文件。它记录源文档引用与内容指纹、结构化数据、来源锚点、视图类型、布局、展示覆盖和导出设置。
- **可视化工作簿 (Visual Workbook)**:一个 `.foliaviz` 文件承载的完整编辑单元，可包含多个共享同一源文档的可视化页签，并支持按当前页或全部页导出。
- **单一来源工作簿 (Single-source Workbook)**:首版一本可视化工作簿只绑定一份 Markdown，但可包含多个可视化页签。工作簿优先保存相对来源路径；来源丢失时由用户重新绑定，不自动合并其他文档。
- **工作簿草稿 (Workbook Draft)**:已生成但尚未选择 `.foliaviz` 保存路径的内存工作簿。首次保存默认使用源 Markdown 同目录、同名文件；正式保存后进入自动保存，关闭未保存草稿必须确认。
- **可视化页签 (Visual Sheet)**:工作簿内的一种独立视图；拥有自己的视图类型、结构、布局、展示覆盖和导出设置，并与其他页签共享来源锚点体系。
- **来源锚点 (Source Anchor)**:可视化元素与 Markdown 原始片段之间的可校验关联。它用于定位原文修正目标；当原文变化导致锚点失效时，不允许静默回写。
- **展示覆盖 (Presentation Override)**:只属于可视化产物的文字或样式调整，例如简称、换行、层级强调和布局，不改变源 Markdown。
- **原文修正 (Source Correction)**:用户从可视化元素发起、经差异预览确认后写回 Markdown 来源片段的内容修改。普通视图编辑默认不触发原文修正。
- **增量更新审阅 (Incremental Update Review)**:源 Markdown 指纹变化后，对新增、修改、删除和来源失效内容进行逐项确认的同步过程。它默认保留人工布局与展示覆盖，不自动重生成整个工作簿。
- **来源失效 (Detached Source)**:原来源锚点因 Markdown 删除或重写而无法校验的视图元素状态。元素不会被静默删除，需由用户删除、保留为独立注释或重新绑定来源。
- **本地可视化引擎 (Local Visualization Engine)**:内置于 Folia、无需网络或外部 Agent 即可完成结构抽取、视图推荐和渲染的转换能力。它可以借鉴已安装 Skill 的稳定方法论，但运行时不读取或调用用户机器上的 Skill。它是抽取通道之一。
- **抽取通道 (Extraction Channel)**:为可视化工作簿产出结构化数据的来源。当前有两个通道：本地可视化引擎（确定性、即时）与 Agent 抽取（智能、非确定、需验证）。所有通道的产物进入同一套锚点验证、待确认降级与编辑管线；通道只决定数据怎么来，不改变工作簿模型。
- **Agent 抽取 (Agent Extraction)**:由 Folia 发起、在应用进程外运行的智能抽取通道：把源文档连同抽取规格交给用户本机已登录的编码 Agent，产出候选工作簿数据。视图类型由 Agent 建议、用户可改；其结果不可直接信任，必须经宽容导入验证后才能进入正式视图。
- **抽取规格 (Extraction Spec)**:随 Folia 版本发布的规格文本，描述从源文档产出什么结构、如何引用原文、哪些内容必须降级为待确认项。它是 Folia 的版本化资源，承担旧"抽取规则"的角色；Agent 只消费规格，不向 Folia 注入可执行代码。
- **宽容导入 (Lenient Import)**:接收外部生产者产出的工作簿数据时的验证过程：以原文摘录为准重新定位来源锚点并补全校验字段；定位成功的元素成为可追溯元素，失败或存疑的降级为待确认项，而不是拒绝整个文件。
- **视图路由器 (View Router)**:根据各视图的数据契约，以确定性本地规则评估源文档与候选视图的匹配程度，并决定直接生成还是先展示候选。相同内容和规则版本必须产生相同结果。
- **视图匹配度 (View Fit)**:源文档满足某种视图数据契约的程度，不是对视觉美观的评分。结果必须附带命中的内容信号、缺失字段和待确认项，禁止只显示不可解释的分数。
- **可追溯元素 (Traceable Element)**:拥有有效来源锚点、可以定位到 Markdown 依据的正式节点、连线、事件或数据项。首版正式视图只接纳可追溯元素。
- **待确认项 (Review Item)**:能定位原文但字段不完整，或仅由规则推测而缺少直接依据的候选内容。推测关系只能进入待确认侧栏，未经用户确认不得成为正式视图元素。
- **视图注释 (Visual Annotation)**:用户在可视化工作簿中手工添加、尚未绑定 Markdown 来源的节点、连线、结论或强调内容。它必须与来源元素明确区分，可以保留在导出中，也可以后续绑定原文并升级为可追溯元素。
- **Skill 模板 (Skill-derived Template)**:从某个可视化 Skill 中提炼并固化到 Folia 的类型定义、抽取规则和渲染模板；它是 Folia 的版本化资源，不是对外部 Skill 的运行时调用。
- **声明式模板包 (Declarative Template Pack)**:未来可导入的安全可视化扩展，只能描述视图家族、字段映射、识别词、布局参数、配色、图例和数据门槛，不得包含 JavaScript、外部 Skill 或其他可执行代码。首版仅提供内置模板。
- **视图家族 (View Family)**:共享一种数据契约和编辑器的可视化类别。首版固定为结构总览、时间轴、关系网络、流程与决策、矩阵与对比、数值图表；具体法律用途作为家族下的模板，不重复建设渲染器。
- **推荐路由 (Recommendation Routing)**:高匹配度时直接生成首选视图并展示备选；低匹配度时先展示两至三个候选及依据，用户确认后生成。完整类型列表收纳在“更多类型”中。
- **成品导出 (Published Export)**:从可视化工作簿生成的只读交付物。首版支持当前页 PNG、适用页签 SVG、当前页或整本 PDF，以及保留页签浏览交互的独立 HTML；`.foliaviz` 是唯一保留编辑和来源同步能力的格式。
- **交付净化 (Delivery Sanitization)**:生成成品导出时不可绕过的隐私处理。它移除源文件路径、内容指纹、内部标识、撤销历史和未显示原文；来源摘录只有在用户显式开启时才进入成品。

## 待定(待访谈澄清)

- **视图类型 (View Type)**:视图家族下的具体语义模板，例如关系网络下的“人物关系”和“资金流向”，矩阵与对比下的“证据—待证事实矩阵”。
