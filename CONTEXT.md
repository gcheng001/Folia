---
name: folia-glossary
description: Folia 一键可视化功能相关术语表(glossary)
---

# Folia 一键可视化 — 词汇表

> 本文件只记录**业务概念**。实现决策、技术栈、模块边界写到 ADR,不在此列。

## 已确认

- **源对象 (Source Document)**:用户当前在 Folia 中查看/编辑的 Markdown 文档整体。"一键可视化"的输入默认指向**整篇文档**,而非某一选区或某一块。
- **结构化中间表示 (Structured Intermediate Representation, SIR)**:从非结构化 Markdown 抽取出的、供渲染层消费的中间结构。任何可视化都先把源对象归约到这里,再决定画什么。
- **可视化产物 (Visual Artifact)**:根据源文档派生出的独立、可编辑图形结果。它不改写 Markdown；事实或原文抽取错误应回到源文档修正，布局、视觉层级、简称和展示文案只修改可视化产物。
- **Skill 成品图 (Skill-rendered Visual)**:由经过选定的可视化 Skill 生成、以视觉质量优先的静态成品。它可以查看、导出和重新生成，但首版不支持逐个拖动或编辑图形元素；它与可编辑的可视化工作簿是两种不同产物。
- **成品图风格 (Rendered Visual Style)**:成品图的整体颜色、边框、背景和统一字体表达，不改变文字、关系或位置。内置浅色正式、简洁商务、深色科技和柔和彩色四种；第二阶段支持预览后切换，并允许把当前搭配保存为自定义风格。
- **成品图类型 (Rendered Visual Type)**:Skill 成品图采用的信息组织形式。提供流程图、时间轴、关系图和成品脑图；软件可以依据当前文档推荐一种，但所有类型必须在生成入口旁保持可见，用户可以在生成前改选。
- **成品图版本 (Rendered Visual Version)**:同一源文档、同一成品图类型多次生成后依次保留的结果。文件名由原文名称、图类型和顺序号组成，不包含风格；新版本不得覆盖旧版本。
- **成品图事实 (Rendered Visual Fact)**:Skill 成品图中关于姓名、身份、日期、金额、原文引述和关系等可核对内容。重新生成可以改变布局、颜色、文字多少和强调重点，但不得改变这些事实的含义；每项事实都应能回到源文档核对。
- **成品图事实核对 (Rendered Visual Fact Check)**:对成品图中的姓名、日期、金额、案号和原文引用等明确内容与源 Markdown 进行的逐项比对。它不判断普通概括、观点或法律结论是否正确；没有绑定原文的旧图必须显示为尚未核对。
- **成品图手动修正 (Rendered Visual Correction)**:用户在第二版编辑能力中直接修改 Skill 成品图的文字、方框位置和大小，新增或删除方框与连接线，已有连接线随方框自动调整。普通修正保存到当前可编辑工作副本，不自动改写源 Markdown；重大操作前自动保留历史版本。
- **成品图局部样式 (Rendered Visual Element Style)**:第二版中针对单个方框或连接线的视觉调整。方框可以改颜色和强调程度，连接线可以改颜色、粗细、实线或虚线以及箭头；字体继续由整张图统一控制，避免局部修改破坏整体一致性。
- **自定义成品图风格 (Custom Rendered Visual Style)**:用户从当前成品图保存的整图颜色、边框、背景和统一字体组合。应用前必须预览并保留历史版本；受手工保护的特殊颜色可由用户选择继续保留。
- **Markdown 脑图 (Markdown Mind Map)**:工具栏中直接以脑图方式编辑当前 Markdown 标题层级的视图。修改节点文字或上下级会更新 Markdown；它不是独立的成品图文件。
- **结构总览 (Structure Overview)**:一键可视化工作簿中用于分析整篇文档结构的视图，可与时间轴、关系图等页签并存。它默认只修改 `.foliaviz` 工作簿，不直接改写 Markdown。
- **成品脑图 (Published Mind Map)**:Skill 成品图中的脑图类型，目标是生成适合展示和交付的独立 SVG。第二阶段可保存为可编辑 Skill SVG，但不等同于直接编辑 Markdown 的 Markdown 脑图。
- **图表编辑内核 (Diagram Editing Core)**:Markdown 脑图、可编辑 Skill SVG 以及工作簿中结构总览、关系图和流程图共用的选择、多选、拖动、缩放、组合、对齐、吸附、撤销、重叠修复、弹性画布、风格和导出能力。各产品继续使用自己的数据来源和保存规则。
- **对照模式 (Source–Visual Comparison Mode)**:同一工作区左侧显示并编辑源 Markdown，右侧显示并编辑对应可视化产物的双栏状态。两边可互相定位来源与图形，但各自按照所属文件独立保存，退出对照不会合并或覆盖两份内容。
- **图表待更新 (Visual Update Pending)**:源 Markdown 已发生变化、但对应可视化尚未经过用户确认更新的状态。它不阻止原文或图表各自保存；用户检查变化范围并执行“更新图表”后才解除。
- **图表变化审阅 (Visual Change Review)**:图表更新前后可选查看的文字、节点和关系变化清单。它用于处理少量重要冲突，不是更新图表的强制步骤；用户可以按既定保护规则快速更新，再按需查看未自动处理的项目。
- **快速更新 (Quick Visual Update)**:把最新原文中能够可靠对应的变化一键应用到当前图，同时保留手工修改保护和无法确定的冲突。完成后只汇总自动更新、受保护和待查看的数量，用户不必逐项审批。
- **实时变化标记 (Live Change Marker)**:源 Markdown 修改后、图表尚未更新前，对受影响图形、已删除来源和未进入图表的新增内容所作的轻量提示。它不改变图表内容或布局，用户可关闭显示。
- **当前对照图 (Active Comparison Visual)**:一份源 Markdown 在对照模式右侧当前显示的单个可视化产物。Folia 按文档记住上次选择；用户可从其他关联图及版本中切换，但右侧不同时并排多张图。
- **对照生成 (In-Comparison Generation)**:用户保持左侧源 Markdown 可读可编辑，同时在右侧开始、观察并完成可视化生成的过程。生成结果直接成为当前对照图；后续原文修正不会覆盖该结果，用户可选择更新当前图或重新生成新版本。
- **生成输入快照 (Generation Input Snapshot)**:用户点击开始生成或重新生成时固定下来的 Markdown 内容版本。正在运行的任务始终使用该快照；其间原文继续变化时，完成结果标记为基于较早原文，而不是混入任务中途的修改。
- **可视化生成队列 (Visual Generation Queue)**:来自一份或多份 Markdown 的待运行生成任务集合。Folia 全局一次只运行一个联网模型任务，其余任务保留各自输入快照和顺序；本地编辑、保存、修复与导出不进入该队列。
- **选区生成 (Selection Visual Generation)**:根据左侧 Markdown 当前选中的连续内容或标题章节生成可视化，而不是处理整篇文档。生成输入仍固定为开始时的选区快照，结果只与所选原文范围建立来源关联。
- **添加到当前图 (Add Selection to Current Visual)**:把左侧选区提取出的新节点和关系加入当前对照图的显式操作。它不同于选区生成独立图；应用前预览新增内容和落点，并在当前图历史中保留操作前状态。
- **优化当前图 (Refine Current Visual)**:第二版中以当前可编辑成品图为基础继续调用 AI 的操作。它默认不能改变受手工修改保护的内容，只调整未被手工修改的部分或用户明确解除保护并指定的部分；操作前自动保留历史版本。
- **重新生成 (Regenerate Visual)**:第二版中忽略当前成品图的手动编辑、重新依据源文档和本次类型与风格选择生成新图的操作。当前图继续保留，新结果按下一版本号保存。
- **可编辑 Skill SVG (Editable Skill SVG)**:第二版同时承担成品展示和 Folia 编辑来源的单一文件。普通看图软件只显示成品，Folia 可以恢复其中的文字、方框、尺寸和连接关系；没有 Folia 编辑信息的普通 SVG 仍按只读图片打开。
- **最终布局权 (Final Layout Authority)**:对成品图中文字换行、元素尺寸、最终坐标和连线路径作出决定的责任。可编辑 Skill SVG 的最终布局权属于 Folia；生成模型只表达内容、关系和设计意图，模型提供的坐标不得越过无重叠与不越界要求。
- **设计意图 (Design Intent)**:生成模型对成品图层级、分组、相对顺序、强调重点和整体风格的表达。它约束 Folia 的本地布局，但不包含必须原样保留的绝对坐标。
- **成品图场景模型 (Rendered Visual Scene Model)**:可编辑 Skill SVG 内嵌的结构化编辑真相，记录节点、文字、关系、设计意图和人工修改状态。Folia 从场景模型恢复编辑，不从可见 SVG 图元反向猜测结构。
- **SVG 展示投影 (SVG Presentation Projection)**:由成品图场景模型生成、供普通看图软件显示的标准 SVG 图形。它与场景模型保存在同一文件中，但不是 Folia 恢复编辑状态的权威来源。
- **旧图升级 (Legacy Visual Upgrade)**:把没有 Folia 编辑信息的既有 SVG 复制为新的可编辑版本，同时优先保持原图外观。能够可靠识别的文字、方框和连接线进入编辑状态；无法可靠识别的部分保持原样并锁定，原文件永不覆盖。
- **锁定旧图元素 (Locked Legacy Element)**:旧图升级时因归属或连接关系无法可靠判断而保留原样的图形部分。它继续正常显示，但在用户明确处理前不参与自动移动、重画或结构编辑。
- **旧图补充识别 (Legacy Element Classification)**:用户把锁定旧图元素明确标记为文字、方框或连接线，或指定哪些图形组成一个节点、哪条线连接哪些节点的可撤销操作。确认后相应部分进入正常编辑状态，不提供可能混淆背景和装饰的一键全部解锁。
- **局部重叠修复 (Local Overlap Repair)**:只调整发生文字、方框或连线碰撞的局部区域，未受影响的节点、标题、颜色和整体构图保持不变。它是旧图和可编辑新图处理重叠时的默认操作。
- **整图重新排版 (Full Visual Relayout)**:在用户明确选择后重新计算整张图的位置与连线。执行前必须提供预览，确认后另存新版本，不替代默认的局部重叠修复。
- **实用图表编辑器 (Practical Diagram Editor)**:用于把 AI 生成图修正、补全、整理并交付的编辑工作区，覆盖文字、节点、连接线、组合、对齐、撤销、布局修复和常用导出。它不是专业美术软件，不提供钢笔路径、手绘画笔、复杂滤镜、逐字字体或图形布尔运算。
- **可编辑工作副本 (Editable Working Copy)**:用户当前持续修改和正常保存的可编辑 Skill SVG。旧静态图升级时先创建工作副本并永久保留原文件；普通保存更新工作副本，不为每个小改动另建文件。
- **成品图历史版本 (Visual History Version)**:整图重新排版、AI 优化和其他重大操作前自动保留的可恢复快照，用户也可以主动创建。它用于回退重要变化，不等同于每次输入或拖动都生成一个桌面文件。
- **手工修改保护 (Manual Edit Protection)**:用户修改过的文字、位置、大小、颜色或连线所获得的持久保护状态。AI 优化默认不得改变受保护内容，只有用户明确解除保护并授权后才能调整；重新生成始终另建新图。
- **成品图质量检查 (Visual Quality Check)**:正式导出前对文字遮挡或溢出、节点遮挡、连线穿过无关内容、画布越界和不可读字号进行的逐项检查。未解决的严重问题阻止正式导出，但不阻止工作副本保存。
- **有意设计例外 (Intentional Design Exception)**:用户对单个质量问题作出的明确确认，表示该处重叠或相交属于有意设计。例外只作用于被确认的具体问题，不存在一键忽略全部质量问题。
- **事实核对例外 (Fact Check Exception)**:用户对单项姓名、日期、金额、案号或引用差异作出的明确确认，表示图中写法是有意保留。它只解除该项导出阻止，不代表 Folia 对整张图的事实或法律判断作出保证。
- **Folia**:面向办案/法律知识工作场景的桌面端 Markdown 工具(Tauri + Vditor + React 19)。它不是一个简单 Markdown 阅读器——已具备脑图画布 (`MindMapPane`)、自由流图层 (`FreeFlowLayer`)、自研预览服务 (`vditorPreviewChromeService`)、Mermaid/KaTeX 内置渲染、Vditor IR 清洗、Word/HTML/PDF 导出管线。
- **独立派生视图 (Derived Visual View)**:不写回源文档的可视化工作区。它保存自身的视图类型、结构、布局和展示文案，并保留与源文档内容的来源关联。
- **Folia 可视化文件 (`.foliaviz`)**:可保存、可移动并可继续编辑的可视化产物文件。它记录源文档引用与内容指纹、结构化数据、来源锚点、视图类型、布局、展示覆盖和导出设置。
- **可视化工作簿 (Visual Workbook)**:一个 `.foliaviz` 文件承载的完整编辑单元，可包含多个共享同一源文档的可视化页签，并支持按当前页或全部页导出。
- **单一来源工作簿 (Single-source Workbook)**:首版一本可视化工作簿只绑定一份 Markdown，但可包含多个可视化页签。工作簿优先保存相对来源路径；来源丢失时由用户重新绑定，不自动合并其他文档。
- **工作簿草稿 (Workbook Draft)**:已生成但尚未选择 `.foliaviz` 保存路径的内存工作簿。首次保存默认使用源 Markdown 同目录、同名文件；正式保存后进入自动保存，关闭未保存草稿必须确认。
- **可视化页签 (Visual Sheet)**:工作簿内的一种独立视图；拥有自己的视图类型、结构、布局、展示覆盖和导出设置，并与其他页签共享来源锚点体系。
- **来源锚点 (Source Anchor)**:可视化元素与 Markdown 原始片段之间的可校验关联。它用于定位原文修正目标；当原文变化导致锚点失效时，不允许静默回写。
- **展示覆盖 (Presentation Override)**:只属于可视化产物的文字或样式调整，例如简称、换行、层级强调和布局，不改变源 Markdown。
- **原文修正 (Source Correction)**:用户从可视化工作簿或可编辑 Skill SVG 的元素发起、经修改前后对比确认后写回 Markdown 来源片段的内容修改。图上普通编辑默认不触发原文修正；无法准确定位原文时不允许自动写回。
- **增量更新审阅 (Incremental Update Review)**:源 Markdown 指纹变化后，对新增、修改、删除和来源失效内容进行逐项确认的同步过程。它默认保留人工布局与展示覆盖，不自动重生成整个工作簿。
- **来源失效 (Detached Source)**:原来源锚点因 Markdown 删除或重写而无法校验的视图元素状态。元素不会被静默删除，需由用户删除、保留为独立注释或重新绑定来源。
- **本地可视化引擎 (Local Visualization Engine)**:内置于 Folia、无需网络或外部 Agent 即可完成结构抽取、视图推荐和渲染的转换能力。它可以借鉴已安装 Skill 的稳定方法论，但运行时不读取或调用用户机器上的 Skill。它是抽取通道之一。
- **抽取通道 (Extraction Channel)**:为可视化工作簿产出结构化数据的来源。当前有两个通道：本地可视化引擎（确定性、即时）与 Agent 抽取（智能、非确定、需验证）。所有通道的产物进入同一套锚点验证、待确认降级与编辑管线；通道只决定数据怎么来，不改变工作簿模型。
- **Agent 抽取 (Agent Extraction)**:由 Folia 发起、在应用进程外运行的智能抽取通道：把源文档连同抽取规格交给用户本机已登录的编码 Agent，产出候选工作簿数据。视图类型由 Agent 建议、用户可改；其结果不可直接信任，必须经宽容导入验证后才能进入正式视图。
- **抽取规格 (Extraction Spec)**:随 Folia 版本发布的规格文本，描述从源文档产出什么结构、如何引用原文、哪些内容必须降级为待确认项。它是 Folia 的版本化资源，承担旧"抽取规则"的角色；Agent 只消费规格，不向 Folia 注入可执行代码。
- **宽容导入 (Lenient Import)**:接收外部生产者产出的工作簿数据时的验证过程：以原文摘录为准重新定位来源锚点并补全校验字段；定位成功的元素成为可追溯元素，失败或存疑的降级为待确认项，而不是拒绝整个文件。
- **视图路由器 (View Router)**:根据各视图的数据契约，以确定性本地规则评估源文档与候选视图的匹配程度，并决定直接生成还是先展示候选。相同内容和规则版本必须产生相同结果。
- **视图匹配度 (View Fit)**:源文档满足某种视图数据契约的程度，不是对视觉美观的评分。结果必须附带命中的内容信号、缺失字段和待确认项，禁止只显示不可解释的分数。
- **可追溯元素 (Traceable Element)**:拥有有效来源锚点、可以定位到 Markdown 依据的正式节点、连线、事件或数据项。首版正式视图只接纳可追溯元素。
- **待确认项 (Review Item)**:能定位原文但字段不完整，或仅由规则推测而缺少直接依据的候选内容。推测关系只能进入待确认侧栏，未经用户确认不得成为正式视图元素。
- **视图注释 (Visual Annotation)**:用户在可视化工作簿中手工添加、尚未绑定 Markdown 来源的节点、连线、结论或强调内容。它必须与来源元素明确区分，可以保留在导出中，也可以后续绑定原文并升级为可追溯元素。
- **Skill 模板 (Skill-derived Template)**:从某个可视化 Skill 中提炼并固化到 Folia 的类型定义、抽取规则和渲染模板；它是 Folia 的版本化资源，不是对外部 Skill 的运行时调用。
- **声明式模板包 (Declarative Template Pack)**:未来可导入的安全可视化扩展，只能描述视图家族、字段映射、识别词、布局参数、配色、图例和数据门槛，不得包含 JavaScript、外部 Skill 或其他可执行代码。首版仅提供内置模板。
- **视图家族 (View Family)**:共享一种数据契约和编辑器的可视化类别。首版固定为结构总览、时间轴、关系网络、流程与决策、矩阵与对比、数值图表；具体法律用途作为家族下的模板，不重复建设渲染器。
- **推荐路由 (Recommendation Routing)**:高匹配度时直接生成首选视图并展示备选；低匹配度时先展示两至三个候选及依据，用户确认后生成。完整类型列表收纳在“更多类型”中。
- **成品导出 (Published Export)**:从可视化工作簿或可编辑 Skill SVG 生成的只读交付物。可编辑成品图支持干净的 SVG、PNG 和 PDF 交付版；工作簿继续支持当前页或整本导出以及独立 HTML。
- **交付净化 (Delivery Sanitization)**:生成成品导出时不可绕过的隐私处理。它移除源文件路径、原文定位信息、内容指纹、内部标识、编辑历史、撤销记录和 AI 过程信息；来源摘录只有在用户显式开启时才进入成品。
- **可编辑保存 (Editable Save)**:把完整编辑状态、历史和必要来源关联保存在可编辑 Skill SVG 中的日常保存操作。它面向继续工作，不等同于可直接对外分享的成品导出。
- **本地编辑链路 (Local Editing Path)**:不依赖网络或外部 Agent 的成品图工作流程，覆盖旧图升级、手工编辑、撤销与历史、重叠检查和修复、重新排版、明确事实比对、保存及交付导出。只有生成新图和 AI 优化当前图属于联网操作。
- **弹性画布 (Elastic Canvas)**:编辑过程中可随内容移出边缘而向外扩展的成品图工作区域。旧图升级时先保持原尺寸；用户可一键收紧到全部内容，导出尺寸变化只调整留白和整体缩放，不暗中重排节点或把文字缩至不可读。

## 待定(待访谈澄清)

- **视图类型 (View Type)**:视图家族下的具体语义模板，例如关系网络下的“人物关系”和“资金流向”，矩阵与对比下的“证据—待证事实矩阵”。
