# Folia 拥有可编辑 Skill SVG 的最终布局权

已接受（2026-07-13）。第二阶段中，生成模型负责成品图的内容、关系、层级、分组、强调重点和整体风格意图；Folia 负责根据实际文字测量决定换行、元素尺寸、最终坐标和连线路径，并在保存成功前执行无重叠与不越界校验。模型坐标最多作为布局建议，不能覆盖 Folia 的碰撞安全要求。该取舍可能改变 Skill 原始构图，但能从根本上消除由模型直接决定像素坐标造成的文字与节点重叠，并为稳定的后续编辑建立统一几何基础。
