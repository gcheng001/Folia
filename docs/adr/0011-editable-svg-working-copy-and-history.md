# 可编辑 SVG 使用工作副本和重大操作历史

已接受（2026-07-13）。旧静态图第一次升级时另建可编辑工作副本并永久保留原文件；普通文字、位置和样式修改通过自动保存或正常保存更新当前工作副本，不为每个小改动制造新的桌面文件。整图重新排版、AI 优化当前图和其他重大操作执行前自动保留可恢复的历史版本，用户也可以随时手动保存新版本。所有文件更新必须采用临时写入、完整性校验后替换的安全流程。该方式在原图安全、日常文件整洁和重要操作可回退之间取得平衡。
