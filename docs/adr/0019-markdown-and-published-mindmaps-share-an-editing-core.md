# Markdown 脑图与可编辑成品图共用图表编辑内核

已接受（2026-07-13）。本次升级不仅建设可编辑 Skill SVG，也把重叠检查与局部修复、对齐吸附、多选组合、撤销重做、弹性画布、整图风格和干净导出等通用能力同步用于 Markdown 脑图，并由两者共用图表编辑内核。Markdown 脑图继续直接编辑 Markdown 标题层级；可编辑 Skill SVG 继续使用内嵌场景模型、旧图升级、事实核对和版本历史，两者不共用文件格式或保存语义。该决定增加前期抽象工作，以避免两套图表编辑器长期产生不同操作手感和重复缺陷。
