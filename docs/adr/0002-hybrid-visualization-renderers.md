# 可视化工作簿采用按内容密度分工的混合渲染器

Folia 的可视化工作簿不使用单一万能画布：结构总览、关系网络与流程决策复用 React Flow 节点画布，时间轴、矩阵与对比使用可编辑 HTML 布局，数值图表使用矢量图表渲染器；所有渲染器共享同一套结构化中间表示、来源锚点、展示覆盖与导出协议。单一 SVG 或节点画布虽然表面统一，但会使法律文档中的长文字、证据矩阵和时间表难以阅读与编辑；混合渲染增加适配层成本，却能按内容性质选择可靠布局，并复用 Folia 已有脑图能力。
