# 内嵌场景模型是可编辑 Skill SVG 的编辑真相

已接受（2026-07-13）。可编辑 Skill SVG 在同一个文件中同时保存版本化的 Folia 成品图场景模型和由它生成的标准 SVG 展示投影；Folia 只从场景模型恢复节点、文字、关系、设计意图和人工修改状态，不从 `<rect>`、`<text>`、`<path>` 等可见图元反向猜测编辑结构。普通看图软件继续直接显示 SVG 投影。该决定牺牲了对外部图形软件修改的自动结构化导入，但避免把脆弱的 SVG 反向解析变成编辑基础，并确保文字测量、重新布局、连线跟随和版本迁移拥有单一权威数据源。
