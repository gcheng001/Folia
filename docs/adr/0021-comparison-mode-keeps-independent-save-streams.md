# 对照模式保持原文与图表独立保存

已接受（2026-07-13）。对照模式把源 Markdown 和对应可视化并排显示并互相定位，但两份文件继续使用各自的自动保存和失败状态；原文变化只把图表标为“待更新”，不在每次输入时自动重画。主工具栏跟随当前获得焦点的一侧切换，避免同时堆放两套操作。只有用户明确执行“更新图表”或经差异确认的“同步到原文”时，两侧数据才发生受控交换。图表变化审阅是可选工具，不得强迫用户在长文档中逐项确认后才能更新。该决定以稳定编辑、人工布局保护和可承受的操作量为先，接受图表可能暂时落后于最新原文。
