# 编辑、修复、核对和导出必须断网可用

已接受（2026-07-13）。可编辑 Skill SVG 的旧图升级、文字与图形编辑、撤销重做、历史恢复、重叠检查与修复、整图重新排版、姓名日期金额案号及引用比对、保存、版本管理和 SVG/PNG/PDF 交付导出全部在 Folia 本机完成，不依赖 Claude、网络或外部 Agent。只有从 Markdown 生成全新成品图和“AI 优化当前图”允许联网调用 Claude。该边界要求 Folia 自己拥有布局与检查能力，但保证网络中断时核心修订和交付工作仍可继续。
