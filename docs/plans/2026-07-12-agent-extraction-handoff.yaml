delivery_handoff:
  outcome: "Folia 工具栏新增「AI 可视化」：headless 唤起本机 claude CLI 按版本化抽取规格生成 .foliaviz 到源文档旁，经宽容导入自动打开为可编辑工作簿；本地一键可视化冻结并修复流程节点星号残留"
  artifacts:
    - "/Users/Apple/Folia-source/docs/plans/2026-07-12-agent-extraction-foliaviz-plan.md"
    - "/Users/Apple/Folia-source/docs/adr/0004-agent-extraction-channel.md"
    - "/Users/Apple/Folia-source/CONTEXT.md"
  acceptance:
    - "打开任一 Markdown 标签点「AI 可视化」（本机 claude CLI 可用时）：源目录出现 <源文件名>.foliaviz，Folia 自动打开且画布元素可编辑"
    - "产物中摘录失配或缺锚点的元素出现在待确认侧栏，文件不被整体拒绝"
    - "claude CLI 缺失、超时（5 分钟）、产物校验失败三类错误各有可区分的用户提示"
    - "AI 入口在 UI 上注明内容将发送给 AI 模型；本地一键可视化行为不变"
    - "流程视图节点标签不再残留 ** 等 Markdown 强调符号"
    - "计划文档中列出的测试、typecheck、eslint 门禁全部通过"
  out_of_scope:
    - "html-anything 宿主入口（C3）与 MyAgents 派发"
    - "claude 之外的 agent CLI 支持"
    - "生成过程流式展示与生成后追问迭代"
    - "本地引擎新增抽取规则；PNG/PDF 导出管线改动"
  repository: "/Users/Apple/Folia-source"
  target_branch: "gaocheng/v0.4-integration"
  checks:
    - "npm test -- --run src/services/visualization/schema.test.ts src/services/visualization/families.test.ts src/services/visualization/sync.test.ts src/components/visualization/VisualWorkbookPane.test.tsx src/app/AppLayout.test.tsx"
    - "npm run typecheck"
    - "npx eslint src/services/visualization src/components/visualization --config config/eslint.config.js"
  authority:
    local_build: true
    package: false
    install: false
    push: false
    pull_request: false
    publish: false
    deploy: false
  routing_overrides: {}
  budget_overrides: {}
